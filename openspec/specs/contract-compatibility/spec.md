## ADDED Requirements

### Requirement: Contract compatibility check
The system SHALL provide a function to check if one workflow's output can feed into another's input.

#### Scenario: Compatibility check
- **WHEN** checking contract compatibility
- **THEN** it returns whether workflows can compose
