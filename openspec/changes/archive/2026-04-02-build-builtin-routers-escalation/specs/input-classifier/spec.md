## ADDED Requirements

### Requirement: ClassifyInput step
The system SHALL provide a ClassifyInput step that classifies input as greeting, actionable, or ambiguous.

#### Scenario: Input classification
- **WHEN** running ClassifyInput step
- **THEN** it returns classification with confidence score
