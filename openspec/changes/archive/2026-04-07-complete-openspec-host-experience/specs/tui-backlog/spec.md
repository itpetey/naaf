## REMOVED Requirements

### Requirement: TUI is explicitly not implemented
**Reason**: The TUI now exists as the primary workflow host, so this requirement no longer reflects the product state.
**Migration**: Use `tui-display` and `workflow-host` specs to define current and future host behaviour.
